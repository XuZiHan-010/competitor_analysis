# 前端环境变量示例 —— 只含 key 名，不含真实值（AGENTS.md 安全红线 1/2）
# 本地用：复制为 .env.local 后填值（.env.local 已被 .gitignore 忽略）

# 后端 API base URL。client.ts 缺省为空串（同域 / Next.js rewrite 代理时留空即可）。
NEXT_PUBLIC_API_BASE_URL=

# LangSmith run 深链模板。必须含 {run_id} 占位符，trace-row 用它跳到“具体那条 run”。
# 不设时 langsmithRunUrl 会回退到 dashboard 根（点进去看不到具体 trace）。
# 取值方法：登录 LangSmith → 打开任一 run → 复制地址栏 URL → 把 run 段换成 {run_id}，例如：
#   https://smith.langchain.com/o/<ORG_UUID>/projects/p/<PROJECT_UUID>/r/{run_id}
# 注意：org/project UUID 不是密钥，但请勿把真实值提交进仓库，只在部署环境（Railway）里配。
NEXT_PUBLIC_LANGSMITH_RUN_URL=

# 可选：报告页 project 级入口。缺省时回退到 RUN_URL 模板的 origin，再退到 dashboard 根。
NEXT_PUBLIC_LANGSMITH_PROJECT_URL=
